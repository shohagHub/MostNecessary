To run if the procedure is in hr db then login to hr db and the run 
And for seeing the output in console issue the command
SQL>set serveroutput on

The run 
@"E:\Education\IITDU\db\PLSQL\hello.sql"